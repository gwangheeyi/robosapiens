# Local Project File Directory (로컬 프로젝트 파일 디렉터리)

`robocontrol`이 내보내는 `.rmfproject` 파일은 이 디렉터리에만 저장됩니다.
앱의 `project에 저장`과 `project에서 열기` 기능은 다른 폴더를 선택하지 않습니다.

같은 프로젝트 이름으로 다시 저장하면 새 사본을 만들지 않고 기존 파일을 안전하게
교체합니다. MySQL의 맵 프로젝트 원장은 이 디렉터리와 별도로 유지됩니다.
